Fixtures under `tests/fixtures/golden/` are carried into this repository for offline compatibility checks.

These captures are committed golden artifacts used for byte-for-byte validation against the copied
`final_only` policy behavior. The scenarios align with the AFPacket compatibility suite and remain
derived from the main PcapConstrictor golden workflows.
